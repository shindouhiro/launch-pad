# 国际化功能使用指南

## ✨ 功能概述

Launch Pad 现已支持中英文双语切换功能！

### 支持的语言
- 🇬🇧 English (英语)
- 🇨🇳 中文 (简体中文)

## 🎯 如何使用

### 1. 在首页切换语言
- 访问 `http://localhost:3000`
- 点击标题旁边的语言切换按钮
- 按钮会显示：
  - 当前为英文时显示 "中文"
  - 当前为中文时显示 "English"

### 2. 在登录页切换语言
- 访问 `http://localhost:3000/admin/login`
- 点击右上角的语言切换按钮

### 3. 在管理后台切换语言
- 登录后，在顶部导航栏右侧可以看到语言切换按钮

## 📝 已翻译的内容

### 公开页面 (/)
- ✅ 页面标题
- ✅ 页面副标题
- ✅ 搜索框占位符
- ✅ 分类标签（全部、生产力、开发、社交、娱乐）
- ✅ 未找到应用提示

### 登录页面 (/admin/login)
- ✅ 页面标题和副标题
- ✅ 表单标签（用户名、密码）
- ✅ 占位符文本
- ✅ 验证错误消息
- ✅ 成功/失败提示

### 管理后台 (/admin)
- ✅ 侧边栏菜单项
- ✅ 退出登录按钮

### 推荐管理页面 (/admin/recommendations)
- ✅ 页面标题
- ✅ 按钮文本（新建、编辑、删除）
- ✅ 表格列标题
- ✅ 表单字段标签
- ✅ 占位符文本
- ✅ 验证错误消息
- ✅ 操作成功/失败提示
- ✅ 确认对话框

## 🔧 技术实现

### 使用的库
- `next-intl` - Next.js 国际化库

### 文件结构
```
apps/portal/
├── messages/
│   ├── en.json          # 英文翻译
│   └── zh.json          # 中文翻译
├── i18n.ts              # 国际化配置
├── app/
│   ├── components/
│   │   └── LanguageSwitcher.tsx  # 语言切换组件
│   └── ...
└── next.config.ts       # Next.js 配置（包含 i18n 插件）
```

### 语言存储
- 用户选择的语言存储在 Cookie 中
- Cookie 名称: `locale`
- 有效期: 1 年
- 切换语言后会自动刷新页面以应用新语言

## 🌐 添加新翻译

### 1. 在翻译文件中添加新的键值对

**en.json**
```json
{
  "common": {
    "newKey": "New Text"
  }
}
```

**zh.json**
```json
{
  "common": {
    "newKey": "新文本"
  }
}
```

### 2. 在组件中使用翻译

```tsx
import { useTranslations } from "next-intl";

export default function MyComponent() {
  const t = useTranslations('common');
  
  return <div>{t('newKey')}</div>;
}
```

## 📋 翻译命名空间

当前使用的命名空间：

- `common` - 通用文本（搜索、分类等）
- `home` - 首页相关
- `admin` - 管理后台相关
- `recommendations` - 推荐管理相关

## 🎨 语言切换按钮样式

语言切换按钮使用 Ant Design 的 Button 组件：
- 图标: `GlobalOutlined` (地球图标)
- 显示当前可切换到的语言名称
- 点击后立即切换并刷新页面

## 💡 最佳实践

1. **保持翻译文件同步**: 确保所有语言文件包含相同的键
2. **使用有意义的键名**: 使用描述性的键名，如 `loginSuccess` 而不是 `msg1`
3. **按功能分组**: 使用命名空间组织翻译，便于维护
4. **测试所有语言**: 切换语言后测试所有功能

## 🚀 未来扩展

可以轻松添加更多语言：

1. 在 `messages/` 目录下创建新的语言文件（如 `ja.json` 日语）
2. 复制现有翻译文件的结构
3. 翻译所有文本
4. 更新语言切换器以支持新语言

## ✅ 功能验证

测试步骤：
1. 访问首页，点击语言切换按钮
2. 验证所有文本都已翻译
3. 访问登录页，切换语言
4. 登录后台，验证菜单和内容翻译
5. 在推荐管理页面测试所有操作的提示消息

所有功能都应该在两种语言下正常工作！🎉
