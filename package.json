{
  "name": "launch-pad-monorepo",
  "private": true,
  "scripts": {
    "dev:portal": "pnpm --filter portal dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:backend": "pnpm --filter backend start:dev"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
